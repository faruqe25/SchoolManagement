@model X.PagedList.IPagedList<SchoolManagement.Areas.Admin.ViewModels.SectionVm>
@using X.PagedList.Mvc.Core

<p class="h2" style="text-align:center;">Class List<br /><div style="border-bottom:    1px solid #686868"></div> </p>

<table class="table table-striped table-responsive">
    <thead>
        <tr>
            <th class="h3">
                Serial
            </th>
            <th class=" h3 ">
                Section Name
            </th>
            <th class=" h3  ">
                Action
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td class=" h4  ">@item.Sl</td>
                <td class=" h4  ">@item.SectionName</td>
                <td class=" h4  "><a asp-action="UpdateSection" asp-controller="Home" asp-area="Admin" asp-route-id="@item.SectionId"><i class="fa fa-pencil-square" style="font-size:25px;color:green"></i></a></td>
            </tr>
        }
    </tbody>
</table>
@Html.PagedListPager(Model, c => Href("~/Admin/Home/SectionList?page=" + c), X.PagedList.Mvc.Common.PagedListRenderOptionsBase.ClassicPlusFirstAndLast)

